Droid Survair - Project Issue Fix Log
Last updated: 2026-06-02

Purpose:
- This file is the shared project memory for important bugs, fixes, and troubleshooting notes.
- Add every future issue fix here so the same problem can be recognized quickly later.
- Keep entries short but complete: issue, root cause, files changed, fix, and verification.

Entry format:

===============================================================================
Date: YYYY-MM-DD
Area: Frontend / Backend / Database / Viewer / Upload / Auth / Other
Issue:
- What was broken?

Root cause:
- Why did it happen?

Fix:
- What was changed?

Files touched:
- path/to/file

Verification:
- What was tested or visually confirmed?

Future note:
- What to remember if this happens again?
===============================================================================


===============================================================================
Date: 2026-06-02
Area: Viewer / Backend / Ortho COG Rendering
Issue:
- Manual synced orthomosaic/ortho COG was not rendering fully in the Leaflet 2D Viewer.
- At different zoom levels it appeared partial/half-rendered.
- White no-data blocks appeared over the basemap.
- Affected file:
  Project_Data/projects/proj_482e52974f060d7e/processed/ortho/Ortho New/Ortho_New_038cab94808d_cog.tif

Root cause:
- Manual synced COG metadata was missing reliable bounds_wgs84 in processing_jobs.json.
- Generic TiTiler info could return source CRS meter bounds such as EPSG:32644, but Leaflet needs WGS84 lat/lng bounds.
- White/bright no-data padding pixels in the TIFF were rendering as opaque.
- Dynamic COG layers could be affected by stale crop/bounds clipping and old cached tile URLs.

Fix:
- Backend calculates WGS84 bounds from the COG source CRS.
- Added/used /api/ortho-cog/bounds?url=... for reliable COG bounds.
- Manual COG sync detects and stores bounds_wgs84, source_crs, and detected_epsg.
- Ortho and orthomosaic layers use the custom /api/ortho-cog renderer.
- Ortho renderer removes edge-connected black/bright/near-white padding by making it transparent.
- Renderer cache version bumped to edge-padding-v7.
- Frontend rejects non-WGS84 bounds from generic TiTiler info fallback.
- Frontend auto-fits selected COG layers using WGS84 bounds.
- Frontend disables Leaflet bounds clipping for dynamic COG tile layers.
- Frontend uses unique Leaflet panes per COG tile URL to avoid stale crop clip-path effects.

Files touched:
- backend/app/main.py
- frontend/src/components/MapViewer/MapViewer.tsx
- Project_Data/processing_jobs.json

Verification:
- Backend compile passed.
- Frontend build passed.
- Direct renderer diagnostics passed for zoom levels z13, z14, z15, z16, z17, and z18.
- Live browser check passed:
  - Ortho_New_038cab94808d.tif displayed fully.
  - Zoom in worked without disappearing.
  - Zoom out worked without disappearing.
  - White no-data block was no longer visible.

Future note:
- For any future Ortho/COG issue, first check bounds_wgs84, source_crs, detected_epsg, tile URL renderer, and no-data transparency.
- If a TIFF has missing/corrupt CRS, it must be assigned or repaired before reliable map placement is possible.
===============================================================================


===============================================================================
Date: 2026-06-02
Area: Admin / Auth / Sidebar Visibility
Issue:
- Admin needed clearer user Data Catalog visibility state and a separate way to hide client sidebar tabs.
- Data Catalog on/off was not visually obvious enough for admins.

Root cause:
- User records only stored can_access_catalog and the admin table had no per-user sidebar tab preference.
- The Dashboard sidebar did not read any hidden-tab list from the logged-in user.

Fix:
- Added users.hidden_tabs as a JSON text column.
- Added admin API to save hidden client tabs.
- Added hidden_tabs to auth/me and admin activity responses.
- Added a Hidden Tabs dropdown in Admin Control for Viewer (2D), Viewer (3D), Compare, and Data Downloads.
- Kept Data Catalog out of the hidden-tabs dropdown because it has its own existing control.
- Made Data Catalog button green when visible/on and red when hidden/off.
- Dashboard sidebar and dashboard module cards now hide blocked tabs for non-admin users.

Files touched:
- backend/app/core/database.py
- backend/app/main.py
- frontend/src/services/adminService.ts
- frontend/src/services/authService.ts
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Admin/AdminDashboard.tsx
- frontend/src/components/Admin/AdminDashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build

Future note:
- Data Catalog access and hidden sidebar tabs are intentionally separate controls.
- Hidden tab IDs are: map, globe, compare, downloads.
===============================================================================


===============================================================================
Date: 2026-06-02
Area: Dashboard / Auth / Admin Visibility
Issue:
- Login showed Request failed (500) after adding hidden tab controls.
- Dashboard dataset metric showed duplicate file-derived counts instead of actual project dataset count.
- Client Data Hub did not visually show when backend processing was active.
- Dashboard had no dedicated 3D Workspace card.

Root cause:
- Running databases could miss the new users.hidden_tabs column until schema migration/restart completed.
- Dashboard metric counted project file rows, which can include raw and processed entries for the same dataset.
- Processing jobs were fetched once and had no active UI state.
- Dashboard modules only had a 2D viewer entry.

Fix:
- Auth login/auth-me now call ensure_tables before reading user visibility columns.
- Migrated both live and dev DB schemas locally.
- Dataset metric now counts unique non-report datasets.
- Dashboard polls project jobs/files and marks Client Data Hub active when jobs are processing.
- Gear icon rotates while active processing exists.
- Added 3D Workspace Viewer dashboard card linked to the 3D viewer.
- Hidden admin tabs continue to hide matching dashboard module cards.

Files touched:
- backend/app/main.py
- frontend/src/services/datasetService.ts
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Dashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build
- Live and dev DB schema migration commands completed.

Future note:
- For dashboard count mismatch, check whether the UI is counting raw file rows or unique dataset IDs.
- For login 500 after schema changes, run ensure_tables/migration for both live and dev DB worlds.
===============================================================================


===============================================================================
Date: 2026-06-02
Area: DevOps / Auth / Live-Dev Isolation
Issue:
- Dev frontend on port 5173 showed Vite proxy ECONNREFUSED for 127.0.0.1:8001 when the dev backend was not running.
- /api/auth/me returned 500 after hidden_tabs was added.
- Live portal could show dev updates because Live preview served frontend\dist and Live backend started directly from backend\ source.

Root cause:
- FastAPI response annotation for auth/me did not allow hidden_tabs list values.
- Live and dev had separate ports/databases, but not fully separate deployed frontend/backend artifacts.

Fix:
- Widened auth/me and admin activity response types to allow list/object values.
- Confirmed dev backend 8001 is listening and /api/version responds.
- Confirmed /api/auth/me returns 401 without cookie instead of 500.
- Added VITE_BUILD_OUT_DIR support.
- Live frontend now serves frontend\dist_live.
- Live backend now runs from Droid_Environment_Manager\Live_Backend_Release\backend.
- 2_Start_Live_Portal.bat starts existing deployed artifacts and only creates them once if missing.
- 3_Nightly_Auto_Deploy.bat refreshes dist_live and Live_Backend_Release.

Files touched:
- backend/app/main.py
- frontend/vite.config.ts
- Droid_Environment_Manager/2_Start_Live_Portal.bat
- Droid_Environment_Manager/_Run_Live_Frontend_4173.bat
- Droid_Environment_Manager/_Run_Live_Backend_8000.bat
- Droid_Environment_Manager/3_Nightly_Auto_Deploy.bat
- Droid_Environment_Manager/ARCHITECTURE_REFERENCE.txt
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build
- GET http://127.0.0.1:8001/api/version returned dev version JSON.
- GET http://127.0.0.1:8001/api/auth/me without cookie returned 401.

Future note:
- Dev frontend 5173 requires dev backend 8001 to be running.
- Do not use normal frontend\dist for Live. Live must use frontend\dist_live.
- Do not run Live backend from backend\ source. Live must use Live_Backend_Release.
===============================================================================


===============================================================================
Date: 2026-06-04
Area: Upload / Backend / Deployment
Issue:
- Live point-cloud upload failed at the first chunk.
- Browser console showed 429 Too Many Requests for /api/upload-chunk and transient 502 responses.
- Deployment script could only wait until 3:00 AM.

Root cause:
- The generic heavy-request rate limiter was also applied to chunk-upload endpoints.
- Large point-cloud files legitimately send many sequential chunk requests, exceeding the generic request limit.

Fix:
- Exempted authenticated /api/upload-chunk and /api/upload-dataset-chunk requests from the generic heavy-request limiter.
- Kept authentication, project access, disk capacity, completion, and processing checks active.
- Added Deploy Now or Schedule for next 3:00 AM selection to 3_Nightly_Auto_Deploy.bat.

Files touched:
- backend/app/main.py
- Droid_Environment_Manager/3_Nightly_Auto_Deploy.bat
- Droid_Environment_Manager/ARCHITECTURE_REFERENCE.txt
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Code changes applied in Dev source.
- Live release was intentionally not updated automatically; deploy using the updated script.

Future note:
- Never apply a low generic request-per-minute limit to individual upload chunks.
- Use 3_Nightly_Auto_Deploy.bat and choose N for immediate deployment or S for the next 3:00 AM.
===============================================================================


===============================================================================
Date: 2026-06-04
Area: 3D Viewer / Potree
Issue:
- A point-cloud dataset could fall back to the Cesium 3D viewer instead of opening Potree.
- The reported live point-cloud upload had failed at 0%, so no Potree output existed to open.

Root cause:
- Dashboard Potree routing required the active layer URL to end exactly with .html.
- Valid PointCloud/Potree entry URLs with query strings or alternate paths did not satisfy that strict suffix check.
- Failed chunk uploads cannot generate the Potree viewer output.

Fix:
- Potree routing now detects PointCloud/Potree layer types and pointcloud/potree viewer URLs instead of requiring an exact .html suffix.
- Point-cloud chunk upload rate-limit fix remains required before re-uploading the failed file.

Files touched:
- frontend/src/components/Dashboard.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Source routing fix applied in Dev.
- Live deployment/restart must be run before testing the failed point-cloud upload again.

Future note:
- If Potree does not open, first confirm the upload completed and PotreeConverter output exists.
- Point-cloud routing must be based on the layer type, not only an exact URL suffix.
===============================================================================


===============================================================================
Date: 2026-06-04
Area: 3D Viewer / Potree / Cesium
Issue:
- The project 3D workspace did not provide one clear list of all project point clouds and 3D models.
- Users could not explicitly choose Potree versus Cesium from the 3D workspace.

Fix:
- Added a project-scoped 3D asset sidebar to Viewer (3D).
- Point Clouds and 3D Models render as separate sections.
- Empty sections are not displayed.
- Clicking a Point Cloud opens its layer in Potree.
- Clicking a 3D Model switches the workspace to Cesium.
- Asset detection uses dataset type, layer type, file name, and viewer URL.

Files touched:
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Dashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Dev source changes applied.
- Build command could not execute because the local command approval service timed out.
- Live release was intentionally not updated automatically.

Future note:
- Keep PointCloud/Potree and 3DModel/Cesium routing explicit in the shared 3D workspace sidebar.
===============================================================================


===============================================================================
Date: 2026-06-04
Area: 3D Viewer / Asset Discovery
Issue:
- Existing processed Potree point clouds were not appearing in the new Viewer (3D) sidebar.

Root cause:
- The sidebar only loaded project file entries.
- Potree viewer URLs can be stored in processing job result_url instead of project file layer_url.
- The asset list did not refresh when reopening Viewer (3D) after processing completed.

Fix:
- Viewer (3D) now combines 3D assets discovered from project files and processing jobs.
- Point-cloud jobs with Potree result URLs are included in the Point Clouds section.
- Viewer (3D) refreshes the project asset list whenever the 3D tab is opened.
- 3D Models continue to appear conditionally and open the Cesium viewer.

Files touched:
- frontend/src/components/Dashboard.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Dev source fix applied.
- Build/deploy command could not execute because the local command approval service timed out.
- Live portal still requires Deploy Now before this correction is visible on port 4173.

Future note:
- Discover processed Potree assets from both project files and processing job result URLs.
===============================================================================


===============================================================================
Date: 2026-06-04
Area: 3D Viewer / UI / Branding
Issue:
- The point-cloud rendering engine displayed its default sidebar, controls, and branding.
- The standard portal sidebar did not provide a focused 3D asset workflow.

Fix:
- Added a clean PointCloudViewer wrapper that uses the existing renderer only as a background engine.
- Default renderer sidebar, branding, logo, and map controls are strictly hidden.
- Renderer canvas is forced to fill the available 3D viewer area without a sidebar gap.
- Added a dedicated Viewer3DSidebar that replaces the normal portal sidebar in 3D mode.
- Added Back to Map, Point Clouds, and conditional 3D Models tabs.
- Point-cloud selection opens the clean point-cloud viewer; model selection opens Cesium.
- Applied Deep Teal, Montserrat, and protected Font Awesome icon styling.

Files touched:
- frontend/src/components/GlobeViewer/PointCloudViewer.tsx
- frontend/src/components/GlobeViewer/PointCloudViewer.css
- frontend/src/components/GlobeViewer/Viewer3DSidebar.tsx
- frontend/src/components/GlobeViewer/Viewer3DSidebar.css
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Dashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Dev source changes applied.
- Build command could not execute because the local command approval service timed out.
- Live release was intentionally not updated automatically.

Future note:
- Keep the point-cloud rendering engine visually silent; all navigation and branding must come from Droid Cloud UI.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: DevOps / Live-Dev Isolation / Database Safety
Issue:
- Live release artifacts were still stored inside the Dev project tree.
- The desired operating model is that all code edits happen only in Dev, and Live updates happen only through BAT deployment scripts.

Root cause:
- Live frontend/backend release folders were previously under the Dev workspace, making the separation less obvious.

Fix:
- Created external Live release folder:
  D:\1_Portal_Workflows_development\DroidSurvair_Live_Release
- Live frontend now serves from:
  D:\1_Portal_Workflows_development\DroidSurvair_Live_Release\frontend\dist_live
- Live backend now runs from:
  D:\1_Portal_Workflows_development\DroidSurvair_Live_Release\backend
- Project_Data stays inside the Dev project folder.
- Dev startup still snapshots live DB droid_cloud_prod.db into disposable droid_cloud_dev.db.
- Deploy scripts copy code/builds to Live only and never copy dev DB data into live DB.

Files touched:
- Droid_Environment_Manager/2_Start_Live_Portal.bat
- Droid_Environment_Manager/3_Nightly_Auto_Deploy.bat
- Droid_Environment_Manager/_Run_Live_Backend_8000.bat
- Droid_Environment_Manager/_Run_Live_Frontend_4173.bat
- Droid_Environment_Manager/ARCHITECTURE_REFERENCE.txt
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- External Live folder structure was created with frontend, backend, and logs folders.
- Script references now point to DroidSurvair_Live_Release for Live code artifacts.

Future note:
- Dev DB may be refreshed from Live DB on dev startup.
- Dev DB must never be copied back into Live DB.
- Live code changes should happen only through 3_Nightly_Auto_Deploy.bat.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Projects / Auth / Update UX
Issue:
- Admin needed a project delete option directly from the project directory.
- Users should not be logged out quickly after login.
- Update refresh should not waste time by dropping the user back at the beginning of the portal.

Fix:
- Added admin-only DELETE /api/admin/projects/{project_id}.
- Project delete removes project DB records, spatial data records, camera/crop records, processing jobs, cache, and project storage folders.
- Added admin-only Delete button on project cards with confirm + typed DELETE safety.
- Session TTL is guarded to never be below 24 hours.
- Workspace tab, viewer tab, and selected project ID are saved to localStorage.
- After an update refresh, the portal restores the user to the same project/workspace when possible.

Files touched:
- backend/app/main.py
- frontend/src/services/projectService.ts
- frontend/src/hooks/useProjects.ts
- frontend/src/hooks/useWorkspaceState.ts
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Dashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build

Future note:
- Project delete remains admin-only.
- Update refresh may still reload the app shell, but workspace/project context should restore after reload.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Admin / Projects / User Controls
Issue:
- Admin Projects view needed all users' projects with owner/user identity visible.
- Admin Control needed easy user password reset.
- Data Catalog visibility needed to move into the Hidden Tabs dropdown instead of a separate green/red button.

Fix:
- Added GET /api/admin/projects to return all projects with owner_user_id and owner_email.
- Admin Projects tab now shows all user projects when admin is not managing a specific user.
- Project cards show owner email and user ID for admins.
- Added PATCH /api/admin/users/{user_id}/password for admin password reset and session invalidation.
- Added Reset Password action in Admin Control.
- Added Data Catalog to the hidden tabs dropdown.
- Removed the separate visible/hidden Data Catalog column from Admin Control.
- Data Catalog dropdown toggle keeps the legacy can_access_catalog flag and hidden_tabs in sync.

Files touched:
- backend/app/main.py
- frontend/src/services/projectService.ts
- frontend/src/hooks/useProjects.ts
- frontend/src/services/adminService.ts
- frontend/src/components/Admin/AdminDashboard.tsx
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Dashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build

Future note:
- Data Catalog hiding is now controlled from the Hidden Tabs dropdown.
- Admin password reset signs out that user by deleting their sessions.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Admin / Data Catalog / User Upload Permission
Issue:
- Users could view Data Catalog, but upload access was only admin-only with no separate admin switch.
- Data Catalog visibility and upload permission needed to be independent controls.

Fix:
- Added users.can_upload_data SQLite column with default off for normal users.
- Added can_upload_data to auth session/user responses and admin activity rows.
- Added PATCH /api/admin/users/{user_id}/upload-access for admin-controlled user upload access.
- Updated upload middleware so admins and upload-enabled users can use upload endpoints, while manual sync/metadata admin tools stay admin-only.
- Added User Upload green/red toggle in Admin Control.
- Updated Data Catalog UI so upload controls appear for admins or upload-enabled users, while read-only users can still view/download data.

Files touched:
- backend/app/core/database.py
- backend/app/main.py
- frontend/src/services/authService.ts
- frontend/src/services/adminService.ts
- frontend/src/components/Admin/AdminDashboard.tsx
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Datasets/DatasetsPanel.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build

Future note:
- Data Catalog hidden/off controls tab visibility.
- User Upload on/off controls whether a non-admin can upload data into allowed projects.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Responsive Layout / Admin Control / Viewer Panels
Issue:
- Admin Control hidden-tab dropdown could be clipped inside the scrollable table on Mac/laptop screens.
- Wide admin/data tables did not clearly indicate that more columns were available horizontally.
- Laptop-width layouts needed tighter sidebar/topbar/viewer controls so workspace content remained visible.

Fix:
- Changed Admin Control hidden-tab dropdown to expand inline instead of absolute-positioning inside the scroll container.
- Added left/right reveal indicators to the Admin Control table and a right reveal indicator to Data Catalog table scrolling.
- Added laptop responsive breakpoints for dashboard sidebar, topbar, profile actions, map shell height, overview cards, and module grids.
- Added compact laptop breakpoints for 2D viewer and 3D viewer control panels.
- Updated Compare and Downloads panels for better laptop and 4K behavior.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/components/Dashboard.css
- frontend/src/components/Datasets/DatasetsPanel.css
- frontend/src/components/MapViewer/MapViewer.css
- frontend/src/components/GlobeViewer/GlobeViewer.css
- frontend/src/components/Compare/ComparePanel.css
- frontend/src/components/Downloads/DownloadsPanel.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Not run in this turn because the local shell approval/usage limit blocked further command execution.

Future note:
- If another screen clips content, first check for absolute-positioned menus inside overflow:auto containers.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Full Desktop Responsive Coverage
Issue:
- Portal needed to remain fully usable across common and premium desktop screens:
  1366x768, 1920x1080, 2560x1440, 2560x1600, 3440x1440, and 3840x2160.
- 1366x768 required compact controls so sidebars/viewers do not overlap.
- 4K/ultrawide screens required readable UI sizing and stretch control.

Fix:
- Added global desktop responsive safety layer in frontend/src/index.css.
- Added 1366x768 compact mode for sidebar, topbar, map shells, dashboard cards, admin table, 2D viewer, 3D viewer, and custom 3D sidebar.
- Added 2K/QHD and 4K scaling rules so controls and fonts do not become too tiny.
- Added ultrawide max-width behavior for non-map content while keeping 2D/3D map viewers full working width.
- Added tighter 2D/3D viewer controls for short-height laptop screens.

Files touched:
- frontend/src/index.css
- frontend/src/components/MapViewer/MapViewer.css
- frontend/src/components/GlobeViewer/GlobeViewer.css
- frontend/src/components/GlobeViewer/Viewer3DSidebar.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Not run in this turn because local shell execution was blocked by the current tool usage limit.

Future note:
- QA should manually check 1366x768 first, then 1920x1080, 2560x1600, 3440x1440, and 3840x2160.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Responsive Layout / Admin Table Overlay
Issue:
- Responsive/MacBook preview showed the horizontal scroll arrow indicator floating over Admin Control rows and action buttons.
- User identity column could be partially hidden when the wide table was scrolled horizontally.

Fix:
- Replaced floating arrow indicators with subtle left/right edge fade shadows that do not cover buttons or table content.
- Applied the same non-overlapping edge hint pattern to Data Catalog table scrolling.
- Made the first Admin Control user column sticky so the user/email context stays visible while scrolling wide columns.
- Tightened action, upload, and hidden-tab control sizing for laptop-width responsive views.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/components/Datasets/DatasetsPanel.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Dev Proxy / Failed To Fetch
Issue:
- Frontend could show "Failed to fetch" when the Vite dev server pointed at the wrong backend port or the backend was not ready.
- The Vite config defaulted to backend port 8000 unless an environment variable explicitly set 8001, which made direct dev starts fragile.

Fix:
- Updated frontend/vite.config.ts so Vite dev server defaults to backend 8001 and live preview/build defaults to backend 8000.
- Existing VITE_BACKEND_PORT/BACKEND_PORT overrides still work.
- Updated frontend API service network errors to use the clearer "Cannot reach the API at ..." diagnostic instead of raw browser "Failed to fetch".

Files touched:
- frontend/vite.config.ts
- frontend/src/services/api.ts
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
- Backend compile passed: python -m compileall backend/app
- Dev backend health passed: http://127.0.0.1:8001/api/version
- Dev frontend proxy health passed: http://127.0.0.1:5173/api/version

Future note:
- If localhost:4173 is used, live backend on 8000 must also be running.
- If localhost:5173 is used, dev backend on 8001 must be running.
===============================================================================


===============================================================================
Date: 2026-06-09
Area: API Base / Relative Tile Base
Issue:
- App showed: "Cannot reach the API at ." because API_BASE became an empty string.
- Root cause was frontend/.env.local using VITE_S3_TILE_BASE_URL=/tiles.
- getApiBaseUrl derived API base by removing trailing /tiles; for relative "/tiles" that resulted in "".

Fix:
- Updated frontend/src/lib/apiBase.ts so relative tile bases like /tiles use the browser origin as API base.
- Added fallback when removing /tiles would produce an empty API base.

Files touched:
- frontend/src/lib/apiBase.ts
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
- Dev backend health passed: http://127.0.0.1:8001/api/version
- Dev frontend proxy health passed: http://127.0.0.1:5173/api/version
===============================================================================


===============================================================================
Date: 2026-06-09
Area: Point Cloud Upload / Chunk Network Resilience
Issue:
- Large LAS point cloud upload could fail around early progress with "Cannot reach the API at http://localhost:5173".
- Backend/proxy health was OK and an 8MB multipart route probe reached the backend, indicating transient chunk upload/network interruption rather than a dead API.
- Point cloud upload did not have retry handling, unlike large raster dataset upload.

Fix:
- Reduced point cloud chunk size from 10MB to 8MB for better dev proxy stability.
- Added retry handling for point cloud chunk uploads with 8 attempts per chunk.
- Retry status now shows the chunk number and attempt count instead of failing immediately.
- Non-retryable API errors still stop the upload with the backend error detail.

Files touched:
- frontend/src/context/UploadContext.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
- Dev backend health passed: http://localhost:5173/api/version
- 8MB multipart /api/upload-chunk probe reached backend via Vite proxy and returned expected unauthenticated 403.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Point Cloud Upload / Large LAS-LAZ Conversion
Issue:
- Large point cloud uploads could show frontend timeout while Potree conversion was still running.
- 20GB+ point cloud conversion can take several hours depending on CPU, disk speed, and PotreeConverter.

Fix:
- Confirmed point cloud chunk uploads bypass the generic heavy request rate limiter.
- Confirmed backend merges chunks using streaming copy and starts Potree conversion in background after merge.
- Extended frontend point cloud conversion polling window from 2 hours to 24 hours.
- Updated processing status text for long-running Potree conversions.
- Confirmed project files endpoint lists completed Potree HTML viewers from projects/<id>/processed/<pointcloud>/*.html.

Files touched:
- frontend/src/context/UploadContext.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build

Future note:
- 20GB+ uploads still require enough disk space for uploaded chunks, merged LAS/LAZ, and Potree output.
- PotreeConverter must be installed/configured via POTREE_CONVERTER_EXE or C:\PotreeConverter\PotreeConverter.exe.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Responsive Layout / Remove Table Visual Hints
Issue:
- Admin Control still showed a faint vertical line in responsive preview after earlier sticky/fade fixes.
- The line was caused by responsive table helper styling, not application data.

Fix:
- Removed Admin Control table pseudo edge fades completely.
- Removed Admin Control sticky first-column behavior completely.
- Removed Data Catalog table pseudo edge fade completely.
- Portal now relies on the native horizontal scrollbar with no custom visual overlay helpers.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/components/Datasets/DatasetsPanel.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
- Dev backend /api/version on 127.0.0.1:8001 responded successfully after startup.
===============================================================================


===============================================================================
Date: 2026-06-08
Area: UI States / Hover And Active Button Contrast
Issue:
- Some buttons, especially Admin Control ghost/reset actions, became low-contrast on hover.
- Hover states needed consistent Droid teal variants, with red reserved for Admin Control and destructive/highlight actions.

Fix:
- Added Admin Control color variables for Droid teal, teal hover, soft teal, and red variants.
- Normalized Admin primary, ghost, upload toggle, hidden tabs, approval, role, and destructive button hover states.
- Updated Dashboard sidebar/share/delete button states for clearer active/hover contrast.
- Updated Data Catalog, 2D Viewer, 3D Viewer, Compare, and Downloads button hover states so text remains visible.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/components/Dashboard.css
- frontend/src/components/Datasets/DatasetsPanel.css
- frontend/src/components/MapViewer/MapViewer.css
- frontend/src/components/GlobeViewer/GlobeViewer.css
- frontend/src/components/Compare/ComparePanel.css
- frontend/src/components/Downloads/DownloadsPanel.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Responsive Layout / Overall Visual QA
Issue:
- Overall responsive review found Admin Control hero/card stretching vertically on large responsive previews.
- 4K/ultrawide global max-width could constrain map viewer workspace more than intended.

Fix:
- Set Admin Control layout rows to auto header + flexible table area, preventing oversized header-card stretch.
- Removed global 4K ds-content max-width so 2D/3D map viewers can use full professional screen width.
- Kept ultrawide max-width controls scoped to non-map content only.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- frontend/src/index.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-08
Area: Responsive Layout / Admin Table Sticky Column
Issue:
- MacBook responsive preview showed a vertical shadow/line behind Hidden Tabs after the first Admin Control column became sticky.

Fix:
- Removed the sticky first-column box-shadow and replaced it with a very light border.
- Reduced table edge fade opacity so responsive scroll hints do not look like vertical overlays.

Files touched:
- frontend/src/components/Admin/AdminDashboard.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-09
Area: 3D Viewer / Asset Sidebar Duplicates

Issue:
- Clicking Point Clouds and 3D Models tabs showed repeated copies of the same project assets.
- The same Potree/Cesium dataset could arrive from both project files and processing jobs with slightly different URLs/IDs, so the sidebar received duplicates.

Fix:
- Added canonical 3D asset dedupe keys in Dashboard based on viewer type and normalized processed/pointcloud/tileset paths.
- Merged file and job assets through the canonical key instead of raw URL only.
- Added a defensive sidebar-level unique filter so repeated asset props still render once.

Files touched:
- frontend/src/components/Dashboard.tsx
- frontend/src/components/GlobeViewer/Viewer3DSidebar.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-09
Area: 3D Viewer / Data Catalog Targeted Opens

Issue:
- Clicking an uploaded LAS point cloud could send the raw /data/projects/.../raw/*.las URL into the Potree iframe.
- Raw /data paths are intentionally hidden by backend security middleware, so the browser saw a 404 even though the file existed.
- Data Catalog "Show/Open" buttons used toggle behavior, so clicking a dataset could remove it instead of opening that exact item.

Fix:
- Prevented raw .las/.laz URLs from becoming 3D sidebar Potree assets.
- Prioritized converted Potree processed viewer URLs over raw point cloud URLs when merging 3D assets.
- Data Catalog now uses upsert/open behavior for dataset actions instead of toggle/off behavior.
- Point cloud open action resolves the ready Potree viewer URL through /api/pointcloud-status when a raw or non-viewer URL is encountered.
- 3D viewer now prioritizes the latest clicked 3D layer so Point Cloud opens Potree and 3D Model opens Cesium correctly.

Files touched:
- frontend/src/components/Dashboard.tsx
- frontend/src/components/Datasets/DatasetsPanel.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
- Confirmed converted Potree HTML exists under Project_Data/projects/proj_482e52974f060d7e/processed/.
===============================================================================


===============================================================================
Date: 2026-06-09
Area: 3D Viewer / Potree Authentication In Iframe

Issue:
- Potree iframe showed {"detail":"Authentication required"} after selecting a point cloud from the 3D sidebar.
- Root cause was absolute backend URLs such as http://127.0.0.1:8001/data/... being used as iframe src while the app ran on http://localhost:5173.
- The iframe request did not carry the current localhost session cookie to the backend origin.

Fix:
- Normalized 3D asset URLs in Dashboard through toSameOriginBackendUrl().
- Potree and Cesium assets now open as same-origin /data/... URLs through the frontend proxy/session path.

Files touched:
- frontend/src/components/Dashboard.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-09
Area: 3D Viewer / Point Cloud Open Performance

Issue:
- Switching from Data Catalog to 3D Viewer could trigger extra project files/jobs refetching.
- Potree iframe had no explicit eager-load hint for selected local viewer HTML.

Fix:
- Removed active tab from the 3D asset loading dependency so project 3D assets are loaded by project change, not every tab switch.
- Added same-origin document prefetch hints for the first project point cloud viewer URLs when entering 3D mode.
- Marked the Potree iframe as eager loading for the selected viewer.

Files touched:
- frontend/src/components/Dashboard.tsx
- frontend/src/components/GlobeViewer/PotreeViewer.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-09
Area: Auth / Potree Point Cloud Session Stability

Issue:
- While viewing Potree point clouds, internal files such as hierarchy.bin returned 401 Unauthorized.
- The iframe could show {"detail":"Session expired"} even though the portal topbar still looked logged in.
- Root cause was old DB session expiry combined with many authenticated Potree asset requests.

Fix:
- Added sliding session renewal in backend _require_user().
- Added a grace window so recoverable old sessions can be revived instead of failing during active use.
- Added a short in-memory auth cache to reduce repeated DB auth checks while Potree loads many small .bin/.js files.
- Cleared the auth cache after login, logout, password reset, role/permission updates, and user deletion.
- Added private browser cache headers for processed project data and pointcloud assets to improve repeated point cloud loads.

Files touched:
- backend/app/main.py
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
===============================================================================


===============================================================================
Date: 2026-06-09
Area: 2D Viewer / Digitization Marker And Ownership Permissions

Issue:
- Digitization point markers could save but the marker icon did not render reliably because Leaflet default marker image assets were not always resolved by the Vite bundle.
- Spatial shapes had owner_user_id in SQLite, but edit/delete permissions were not exposed to the frontend or enforced per feature.
- A normal user could attempt to edit/delete admin-created plot/marker shapes from the UI.

Fix:
- Replaced default Leaflet point marker icon with a custom Font Awesome location-dot div icon styled by structure color.
- Added owner_user_id, can_edit, and can_delete fields to spatial feature API responses.
- Backend now allows spatial feature edit/delete only for the creator or an admin.
- Backend now allows spatial layer delete only for the layer creator or an admin.
- Spatial assignment modal is read-only for features the current user cannot edit, and hides delete when delete is not allowed.
- Geometry drag/edit handles only appear for editable selected features.
- Frontend handlers also guard against unauthorized edit/delete attempts and show clear messages.

Files touched:
- backend/app/main.py
- frontend/src/components/MapViewer/spatialTypes.ts
- frontend/src/components/MapViewer/SpatialFeatureLayer.tsx
- frontend/src/components/MapViewer/SpatialAssignmentModal.tsx
- frontend/src/components/MapViewer/MapViewer.tsx
- frontend/src/components/MapViewer/Digitization.css
- frontend/src/services/spatialDataService.ts
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-16 11:55 IST
Area: Point Cloud / Untwine DLL Runtime Path Fix

Issue:
- During EPT fallback conversion, Windows showed:
  "untwine.exe - System Error"
  "The code execution cannot proceed because pdal_util.dll was not found."
- Root cause:
  untwine.exe was launched directly from:
  C:\Program Files\QGIS 3.22.8\apps\qgis-ltr\untwine.exe
  but its required DLL dependency was in:
  C:\Program Files\QGIS 3.22.8\bin\pdal_util.dll
  and that QGIS bin folder was not included in the subprocess PATH.

Fix:
- Backend Untwine launcher now detects the QGIS install root from the untwine.exe path.
- It prepends these folders to PATH before running Untwine:
  C:\Program Files\QGIS 3.22.8\apps\qgis-ltr
  C:\Program Files\QGIS 3.22.8\bin
  C:\Program Files\QGIS 3.22.8\apps\Qt5\bin when present
- This lets Untwine find pdal_util.dll and other QGIS runtime dependencies.

Files touched:
- backend/app/main.py
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app

Next step:
- Restart the backend after this code change, then retry point cloud conversion.
===============================================================================


===============================================================================
Date: 2026-06-16 11:45 IST
Area: Point Cloud / COPC Upload Diagnosis And Cache Reuse Fix

Issue:
- After uploading a LAS, Data Catalog did not clearly show a new COPC output.
- Screenshot showed frontend proxy timeout for /api/upload-chunk.
- Backend processing_jobs showed latest CS-New upload was marked Completed but reused an older EPT output folder with the same file content hash.
- The output folder source marker still had the old source filename, so Data Catalog could show confusing duplicate/raw LAS rows.
- Current terminal PATH did not contain pdal.exe.
- QGIS PDAL was found at:
  C:\Program Files\QGIS 3.22.8\bin\pdal.exe
- That PDAL version runs, but its driver list did not show writers.copc, so it may not be capable of true COPC output. In that case the portal correctly falls back to Untwine EPT.

Fix:
- Backend now includes COPC output folders when hiding duplicate raw LAS rows.
- Backend now also treats completed pointcloud processing jobs as ready names, so re-uploaded same-content raw LAS rows are not shown as separate Web-Ready entries.
- Same-content cache reuse now only reuses existing COPC output. Existing old EPT output is no longer silently treated as final when COPC is the primary target; upload will reprocess so COPC can be attempted first.
- Cached COPC reuse updates .source_name.txt and .viewer_type.txt to the current upload name/type.
- Default PDAL_EXE now points to:
  C:\Program Files\QGIS 3.22.8\bin\pdal.exe
- COPC conversion now checks for writers.copc before running. If the configured PDAL build does not support COPC, backend throws a clear COPC error and falls back to Untwine EPT rather than creating a fake .copc.laz.

Files touched:
- backend/app/main.py
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app

Next check:
- Restart the dev backend so the new code is active.
- Re-upload the same LAS. If installed PDAL still lacks writers.copc, the portal will fall back to EPT and should show viewer_type=ept, not a stuck COPC state.
- For true COPC output, install/configure a PDAL build that exposes writers.copc, then set PDAL_EXE to that pdal.exe before starting backend.
===============================================================================


===============================================================================
Date: 2026-06-16 11:25 IST
Area: Latest Handover Pointer

Note:
- A full project handover snapshot was added in this same file.
- Search inside this file for:
  "Full Project Handover Snapshot / Recent Work Notes Before Codex Reinstall"
- That section records the current project architecture, recent work, COPC/EPT point cloud pipeline, GitHub safety notes, and next manual test steps.
===============================================================================


===============================================================================
Date: 2026-06-16 11:25 IST
Area: Full Project Handover Snapshot / Recent Work Notes Before Codex Reinstall

Purpose:
- This entry is a handover note so the next Codex/session/developer can quickly understand the current Droid Survair Cloud Portal state.
- Keep using this same file for future issue/fix notes. Do not create many separate random notes files.

Project:
- Main workspace:
  D:\1_Portal_Workflows_development\Droid Survair Cloud Portal
- Main stack:
  React + Vite frontend, FastAPI backend, SQLite database, Leaflet 2D viewer, Potree/Cesium style 3D viewer, local Project_Data storage.
- Project data lives under:
  Project_Data/
- Large generated files, uploads, raw LAS/LAZ, TIFF/COG, point cloud outputs, tiles, chunks, and database WAL files should NOT be committed to GitHub.

Environment Architecture:
- Dev world:
  Frontend port 5173, backend port 8001, dev database snapshot.
  Used for normal coding/testing.
- Live world:
  Frontend preview port 4173, backend port 8000, live database.
  Cloudflare tunnel should point to 4173.
- Important database rule:
  Never copy dev database over live database manually.
  Dev can be refreshed from live, but live client data must not be overwritten by testing data.
- Environment scripts live in:
  Droid_Environment_Manager/
- Architecture notes live in:
  Droid_Environment_Manager/ARCHITECTURE_REFERENCE.txt

Important Recent Features Added/Fixes:

1. Digitization and Spatial Data Management:
- Added/updated Leaflet drawing tools for polygons, polylines, and markers.
- Added spatial assignment modal fields:
  Plot ID, Owner Name, Structure Type.
- Added dynamic colors by structure type.
- Added save/load of spatial features/layers in backend SQLite.
- Added KML/SHP import concepts and saved/imported shape list in 2D viewer.
- User-created shapes can be managed by that user; admin-created shapes should stay admin-controlled.
- Admin can manage/delete user data.

2. 2D Ortho/COG TIFF Issues:
- Fixed manual COG/TIFF placement by auto coordinate/EPSG detection logic.
- Fixed nodata transparency and partial/half-disappearing large TIFF display issues.
- Important lesson:
  If future COG/TIFF appears white, half loaded, or changes with zoom, check nodata handling, tile clipping, raster bounds, EPSG detection, and tile serving range/caching first.

3. 2D Compare / Swipe Slider:
- Existing split view was converted into a single-map swipe comparison slider.
- Primary layer renders fully in the background.
- Compare layer sits above it and is clipped by the draggable vertical slider.
- Important fix:
  Do not clip the primary layer. Only clip the compare layer.
  Removing pointer-events none from the compare pane restored interaction on the right side.
- Dropdown layer changes should update comparison automatically without needing slider on/off.

4. Admin Control / Permissions / Responsive UI:
- Admin Control was expanded for:
  Hidden tabs per user.
  User upload on/off.
  User role controls.
  Password reset access.
  Project/user management.
- Data Catalog visibility and user upload permission are separate:
  Data Catalog can be visible while upload is blocked.
- Upload endpoints were updated to allow admins OR users with can_upload_data.
- Responsive fixes were added for laptop/Mac/desktop/4K/ultrawide layouts.
- Use Deep Teal #0e3e49 and Montserrat.
- Use red only for admin/high-risk actions.
- Font Awesome icon protection must remain in CSS:
  font-family: "Font Awesome 6 Free" !important;
  font-weight: 900 !important;

5. Project Delete / Auth:
- Admin-only project delete button/workflow was requested/added around Projects/Admin management.
- Session TTL was increased so logged-in users do not get logged out too quickly.
- Avoid full-page reloads for updates when possible; keep user in current workspace/view.

6. 3D Viewer / Point Cloud Sidebar:
- 3D Viewer has custom Droid sidebar with Point Clouds and 3D Models.
- Potree default branding/sidebar must stay hidden.
- Point cloud list and 3D model list should not duplicate items.
- Data Catalog "Open Point Cloud" should open the 3D viewer directly, not download LAS/LAZ.
- Raw LAS/LAZ serving should remain blocked to avoid data leak/download.

7. Cross Section / L Section:
- 2D viewer has L-section/profile and cross-section work in progress.
- Important distinction:
  L-section/profile follows one selected/drawn/imported line.
  Cross-section uses perpendicular sections along a selected line/alignment.
- Imported KML/SHP/drawn shapes should be selectable from Data Layers & Import, then usable for L-section/cross-section.
- Cross-section export PNG issue was noted and should be checked again.
- Potree reference folder exists for cross-section behavior:
  reference\Potree for Website

8. Standalone Converter:
- A portable standalone conversion UI/workbench was created earlier:
  Droid_Standalone_Converter/
- It has its own installer/start scripts so conversion can run outside the portal.
- Keep output inside that portable folder if using it independently.

9. GitHub Safety:
- Big data files must not go to GitHub:
  Project_Data/, raw LAS/LAZ, TIFF/COG, COPC/EPT outputs, chunks, converted point clouds, logs, database WAL/SHM.
- If GitHub Desktop still shows ignored big files, they may already be tracked in Git history/index.
  Need to remove from tracking without deleting local files:
  git rm --cached <path>
  then commit updated .gitignore and index cleanup.
- GitHub push was blocked by secret scanning:
  Mapbox token found in frontend/public/droid-ept-viewer/libs/.../Cesium.js around line 504.
  Do not bypass secret scan. Remove/replace token and clean it from commit/history if already committed.

Current Latest Work: COPC Primary + Untwine EPT Fallback

Issue:
- Old point cloud conversion had multiple problems:
  PotreeConverter/Entwine/EPT output could fail or create incomplete hierarchy.
  Some generated EPT folders showed "Point cloud is still preparing" forever.
  Some viewer errors included DataView offset/hierarchy decode issues.
  Raw LAS/LAZ clicks could trigger downloads, creating data leak risk.

Implemented on 2026-06-16:
- Backend now tries COPC first:
  PDAL_EXE from environment, default "pdal".
  Command:
  pdal translate "<input_las_file>" "<output_dir>/output.copc.laz"
- If COPC fails or PDAL is not found, backend falls back to Untwine EPT only.
- Entwine fallback is intentionally disabled.
- On COPC success, output folder contains:
  output.copc.laz
  .viewer_type.txt = copc
  .converter.txt = PDAL COPC
  .source_name.txt
- On EPT fallback success, output folder contains:
  ept.json
  ept-data/ept-hierarchy output from Untwine
  .viewer_type.txt = ept
  .converter.txt = Untwine
  .copc_fallback_note.txt when COPC failed first
- On conversion failure, .conversion_error.txt is written and processing job is marked Failed.
- Point cloud status endpoint now detects:
  output.copc.laz first and returns viewer_type "copc", copc_url, tileset_url.
  ept.json second and returns viewer_type "ept", ept_url, tileset_url.
- Data Catalog discovery now lists COPC point clouds as "Droid COPC Point Cloud".
- .copc.laz serving now supports HTTP Range:
  Accept-Ranges: bytes
  Content-Range for 206 responses
  private cache header
- Raw LAS/LAZ in raw/ is still blocked.

Frontend COPC/EPT changes:
- PointCloudStatus type now includes:
  viewer_type: "copc" | "ept"
  copc_url
  ept_url
- Data Catalog URL normalization:
  raw .las/.laz stays blocked.
  processed .copc.laz becomes:
  /droid-ept-viewer/index.html?copc=...&project=...&dataset=...&name=...
  ept.json still becomes:
  /droid-ept-viewer/index.html?ept=...&project=...&dataset=...&name=...
- Point cloud iframe viewer:
  frontend/public/droid-ept-viewer/index.html
  now accepts both query params:
  ?copc=...
  ?ept=...
- For COPC:
  Potree.loadPointCloud(copcUrl, displayName, callback)
- For EPT:
  it still checks/fetches ept.json before loading.
- Upload progress text is now format-neutral:
  "point cloud viewer" instead of only "EPT viewer".
- Viewer error logging area changed to:
  pointcloud_viewer

Files touched by latest COPC work:
- backend/app/main.py
- frontend/public/droid-ept-viewer/index.html
- frontend/src/components/Datasets/DatasetsPanel.tsx
- frontend/src/components/GlobeViewer/PotreeViewer.tsx
- frontend/src/context/UploadContext.tsx
- frontend/src/services/pointCloudService.ts

Other currently modified files in working tree at time of this note:
- Droid_Environment_Manager/ARCHITECTURE_REFERENCE.txt
- Droid_Environment_Manager/_Setup_EPT_Environment.bat
- frontend/src/components/MapViewer/MapViewer.css
- frontend/src/components/MapViewer/MapViewer.tsx

Verification after latest COPC work:
- Backend syntax check passed:
  python -m compileall backend/app
- Frontend production build passed:
  npm.cmd run build
- Vite showed only normal large chunk warning for MapViewer; no build failure.

Next Steps / What To Test Manually:
1. Restart dev backend/frontend.
2. Upload a small LAS/LAZ point cloud.
3. Confirm output folder:
   Project_Data/projects/<project_id>/exports/pointclouds/<dataset_id>/output.copc.laz
4. Check status:
   /api/pointcloud-status/<project_id>?tileset_id=<dataset_id>
   should return viewer_type "copc".
5. Open from Data Catalog:
   It should open in Droid point cloud viewer, not download.
6. Test fallback:
   Temporarily set bad PDAL_EXE and upload tiny LAS/LAZ.
   It should create ept.json through Untwine and return viewer_type "ept".
7. If viewer stays blank:
   Check Project_Data/logs and .conversion_error.txt inside the dataset folder.
8. If GitHub Desktop shows big data:
   Do not push.
   Clean tracked data from git index first.

Important Reminder:
- Do not delete client Project_Data or live database.
- Work/code edits should happen in dev. Live should be updated through controlled script/manual deploy.
- Before major live client use, test point cloud upload/open with one small known-good file and one existing EPT file.
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Point Cloud / Safe Viewer Open And Persistent Error Logs

Issue:
- Clicking a point cloud from Data Catalog could route to a raw LAS/LAZ asset instead of the processed Potree viewer, which risked accidental browser download.
- Failed/partial Potree output folders could still appear as Web-Ready if an HTML file existed beside .conversion_error.txt.
- Potree runtime errors such as "Offset is outside the bounds of the DataView" were only visible in browser console screenshots, not in a persistent project log.

Fix:
- Blocked raw point cloud downloads through dataset raw download and direct project raw data API paths.
- Data Catalog now resolves point cloud clicks through /api/pointcloud-status and only opens a ready Potree HTML/tileset viewer URL.
- Failed/partial point cloud conversion folders are skipped from Web-Ready file listings.
- Added backend JSONL error logging under Project_Data/logs/portal_errors_YYYY-MM-DD.log.
- Added /api/client-error-log for authenticated frontend error reporting.
- Upload failures and Potree iframe runtime/console errors now write to the backend error log.
- Added a responsive viewer error overlay for point cloud runtime decode/load problems.

Files touched:
- backend/app/main.py
- frontend/src/services/errorLogService.ts
- frontend/src/context/UploadContext.tsx
- frontend/src/components/Datasets/DatasetsPanel.tsx
- frontend/src/components/Dashboard.tsx
- frontend/src/components/GlobeViewer/PotreeViewer.tsx
- frontend/src/components/GlobeViewer/PointCloudViewer.tsx
- frontend/src/components/GlobeViewer/PointCloudViewer.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: .\backend\venv\Scripts\python.exe -m compileall backend/app
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Standalone Conversion Workbench / Portal-Compatible External Engine UI

Issue:
- Large Ortho/DTM/DSM and point cloud conversions inside the portal can be slower or more fragile because files go through browser upload/server workflow.
- User needed a separate copyable folder with its own UI to convert local files directly into portal-compatible Project_Data processed folders.

Fix:
- Added Droid_Standalone_Converter folder with a mini FastAPI + static browser UI.
- Copied the existing raster conversion engine into Droid_Standalone_Converter/engine/raster_tiler.py.
- Added GeoTIFF to COG conversion for Ortho/DTM/DSM/DEM with optional manual EPSG assignment.
- Added LAS/LAZ to Potree conversion using PotreeConverter.exe.
- Added automatic LAS bounding-box repair and retry for Potree conversion failures.
- Outputs are written directly to:
  Project_Data/projects/<project_id>/processed/<type>/<dataset_name>/<dataset_name>.cog.tif
  Project_Data/projects/<project_id>/processed/<dataset_name>/<dataset_name>.html
- Added responsive dark teal UI, job progress polling, output paths, logs, requirements, README, and START_CONVERTER_UI.bat.

Files touched:
- Droid_Standalone_Converter/app.py
- Droid_Standalone_Converter/engine/raster_tiler.py
- Droid_Standalone_Converter/static/index.html
- Droid_Standalone_Converter/static/styles.css
- Droid_Standalone_Converter/static/app.js
- Droid_Standalone_Converter/requirements.txt
- Droid_Standalone_Converter/START_CONVERTER_UI.bat
- Droid_Standalone_Converter/README.txt
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Standalone converter compile passed: .\backend\venv\Scripts\python.exe -m compileall Droid_Standalone_Converter
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Standalone Conversion Workbench / Portable Venv Installer

Issue:
- The standalone converter folder did not include an automatic virtual-environment installer for use on another PC.
- Copying an existing .venv between PCs is not reliable because Python package binaries can be machine/Python-version specific.

Fix:
- Added INSTALL_CONVERTER_ENV.bat to create Droid_Standalone_Converter/.venv and install requirements.txt.
- Updated START_CONVERTER_UI.bat to prefer the standalone .venv, then fall back to the portal backend venv, then system Python.
- Updated README with first-time install steps for another PC.

Files touched:
- Droid_Standalone_Converter/INSTALL_CONVERTER_ENV.bat
- Droid_Standalone_Converter/START_CONVERTER_UI.bat
- Droid_Standalone_Converter/README.txt
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Standalone converter compile passed: .\backend\venv\Scripts\python.exe -m compileall Droid_Standalone_Converter
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Point Cloud / Delete Fallback, Runtime Failure Marker, Raw URL Cache Cleanup

Issue:
- Portal error log confirmed Potree runtime failure:
  "Uncaught RangeError: Offset is outside the bounds of the DataView"
  in DecoderWorker.js for May-2026-Point-Cloud-d5c3bae17981.
- Admin Force Delete returned 404 for processed point cloud folders that did not have a matching dataset status row.
- Old browser localStorage/cache could still contain raw .las/.laz point cloud URLs, making the 3D sidebar show raw LAS entries and risking accidental downloads.

Fix:
- Admin dataset delete now falls back to safely deleting processed point cloud folders under:
  Project_Data/projects/<project_id>/processed/<dataset_id>
  and legacy Project_Data/pointclouds/<project_id>/<dataset_id>.
- Potree viewer client error logging now writes .conversion_error.txt into the matching processed point cloud folder and marks the processing job Failed, so corrupt/partial output is not treated as Web-Ready after refresh.
- Point cloud localStorage helper now removes any stored raw .las/.laz URLs.
- Globe viewer option builders now reject raw .las/.laz URLs from file rows, active layers, and stored point cloud entries.

Files touched:
- backend/app/main.py
- frontend/src/utils/pointCloudStorage.ts
- frontend/src/components/GlobeViewer/GlobeViewer.tsx
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: .\backend\venv\Scripts\python.exe -m compileall backend/app
- Frontend build passed: cmd /c npm run build
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Data Catalog / User Upload Permission Enforcement

Issue:
- Admin Control showed User Upload On for a normal user, but point cloud chunk upload still failed with 403: "Admin access required".
- Root cause: middleware allowed can_upload_data users, but the upload endpoint functions still called verify_admin() internally.
- The same mismatch could affect point cloud complete upload, large raster chunk upload, direct dataset upload, and EPSG metadata probing.

Fix:
- Added _require_upload_user() backend helper: admins OR users with can_upload_data are allowed.
- Updated upload-related endpoints to use _require_upload_user():
  /api/upload-chunk, /api/complete-upload, /api/process-pointcloud,
  /api/upload-dataset-chunk, /api/complete-dataset-upload,
  /api/process-dataset, /api/upload-dataset, /api/dataset-metadata.
- Moved /api/process-pointcloud and /api/dataset-metadata from admin-only middleware category to upload-enabled category.
- Kept manual sync/open-folder tools admin-only.

Files touched:
- backend/app/main.py
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
===============================================================================


===============================================================================
Date: 2026-06-10
Area: Point Cloud / LAS Bounding Box Repair And Responsive Upload Errors

Issue:
- Large LAS point cloud conversion failed in PotreeConverter with "encountered point outside bounding box".
- Root cause: the LAS header min/max bounding box was stale/invalid compared with the actual point coordinates.
- The frontend upload progress popup displayed the raw converter log, causing long red text overflow on the screen.

Fix:
- Added automatic LAS/LAZ bounding-box repair fallback before Potree conversion retry.
- If PotreeConverter reports an invalid bounding box, backend now recalculates LAS header min/max from point chunks, writes a temporary repaired LAS, retries conversion once, then removes the temporary repair file.
- Added disk-space guard before creating the temporary repaired copy.
- Added friendly frontend error summaries for point cloud conversion failures.
- Made the upload progress widget responsive with wrapped, scrollable error text and safer mobile sizing.

Files touched:
- backend/app/main.py
- frontend/src/context/UploadContext.tsx
- frontend/src/components/Uploads/UploadProgressWidget.css
- PROJECT_ISSUE_FIX_LOG.txt

Verification:
- Backend compile passed: python -m compileall backend/app
- Frontend build passed: cmd /c npm run build
===============================================================================
2026-06-16 - Point cloud COPC/EPT readiness refinement
- Symptom: Data Catalog showed point clouds as WEB-READY, but opening some LAS/LAZ outputs showed a blank/still-preparing viewer. The expected COPC output folder was empty.
- Root cause: The installed QGIS PDAL build exposes LAS drivers but not the COPC writer, so COPC cannot be produced on this machine yet. The backend correctly falls back to Untwine EPT, but some generated EPT outputs contained only a single root hierarchy JSON, while known-good QGIS EPT outputs contain a chunked multi-file ept-hierarchy. Those single-hierarchy outputs are not reliable for the portal viewer.
- Fix: Backend now scores EPT outputs and treats only chunked, multi-file EPT hierarchy outputs as viewer-ready. Raw LAS/LAZ rows no longer show WEB-READY unless a healthy COPC/EPT viewer asset exists. Data Catalog will avoid exposing raw LAS/LAZ paths as viewer actions.
- Future note: To enable true COPC, install/use a PDAL build with writers.copc support, then set PDAL_EXE before starting the backend. Until then, Untwine EPT fallback is the supported path.

2026-06-16 - Backend startup COPC environment setup
- Symptom: QGIS 3.22.8 OSGeo shell did not expose PDAL writers.copc, but a COPC-capable PDAL was present in the Miniforge droid-ept environment.
- Fix: Droid_Environment_Manager/_Setup_EPT_Environment.bat now auto-detects OSGeo/QGIS, Untwine, and PDAL. It prefers a PDAL executable with writers.copc and exports PDAL_EXE to the backend process. Dev/Live backend scripts already call it, and root start_backend.bat plus droid_manager.bat now call it too.
- Verified: The setup script selected C:\Users\droid\miniforge3\envs\droid-ept\Library\bin\pdal.exe and reported COPC converter support.

2026-06-17 - Manual QGIS EPT point cloud support restored
- Symptom: Manually pasted QGIS EPT folders did not sync/show reliably, and some EPT folders opened blank in the Potree viewer.
- Root cause: The backend was treating single-root ept-hierarchy outputs as incomplete, but QGIS/small EPT datasets can be valid with a single hierarchy JSON. The viewer also forced RGB rendering (`rgba`) for every point cloud, while some QGIS EPT outputs contain Intensity but no Red/Green/Blue dimensions.
- Fix: EPT readiness now accepts valid ept.json + ept-data + at least one ept-hierarchy JSON. Manual Sync now detects EPT/COPC point-cloud folders under processed/pointclouds and processed roots. The viewer chooses RGB when available, otherwise Intensity, Classification, or Elevation so non-RGB EPT clouds render instead of blanking.

2026-06-17 - Prevent EPT point clouds opening as Cesium 3D models
- Symptom: A manually pasted EPT folder such as ept_Stope appeared/opened under the 3D Model/Cesium flow instead of the Potree point-cloud viewer.
- Root cause: Manual sync/model discovery could encounter point-cloud folders while scanning processed folders, and old synced metadata could remain classified as 3DModel.
- Fix: Backend now excludes folders containing ept.json or output.copc.laz from 3D model detection, prioritizes manual point-cloud candidates before model candidates, auto-corrects any existing synced metadata to dataset_type/layer_type pointcloud, and returns explicit layer_type=pointcloud for COPC/EPT project file rows.

2026-06-17 - Potree point-cloud elevation color mode
- Request: Add a DTM-like elevation color schema option inside the Potree point-cloud viewer.
- Fix: The Droid Potree toolbar now includes Natural Color, Elevation Color, and Intensity buttons. Elevation Color switches the material to Potree's elevation attribute, auto-fits the color range from EPT metadata/bounds, and uses Potree's available spectral/rainbow/viridis gradient. Natural Color restores RGB where available or the best fallback attribute for non-RGB EPT point clouds.

2026-06-17 - Potree color buttons visible in React 3D toolbar
- Symptom: Color mode buttons were added inside the iframe viewer template, but they were not visible from the portal's 3D viewer toolbar.
- Root cause: The visible top overlay is React's PotreeViewer toolbar, while the iframe toolbar can be covered/stale depending on cache and layout.
- Fix: React PotreeViewer now exposes Natural, Elevation, and Intensity buttons directly in the visible overlay toolbar. Those buttons call the iframe's color buttons/functions, and the iframe viewer exposes global color helpers for reliable bridging.
- Verification: Frontend build passed: npm run build.

2026-06-17 - Potree Section Box Slice tool
- Request: Add an underground-friendly Potree slice workflow with line-based section box, manual section box, Apply Slice, Clear Slice, Profile CSV, and full clipped-points CSV export.
- Fix: The Droid Potree iframe viewer now exposes Slice Line, Section Box, Apply Slice, Clear Slice, Export Profile CSV, and Export Clipped CSV controls. Slice Line creates a Potree profile and aligns a clip-enabled BoxVolume to it. Section Box uses Potree's native editable BoxVolume insertion. Apply Slice switches Potree clipping to SHOW_INSIDE so only the selected section remains visible.
- React bridge: The visible React Potree toolbar now has matching slice/export buttons and calls iframe bridge functions: droidStartSliceLine, droidStartSectionBox, droidApplySlice, droidClearSlice, droidExportProfileCsv, and droidExportClippedPointsCsv.
- Backend export: Added /api/projects/{project_id}/pointclouds/{dataset_id}/slice-export. It runs an async chunked LAS/LAZ crop against the active section box and writes clipped_points.csv under Project_Data/projects/{project_id}/exports/pointcloud_slices/{dataset_id}/{job_id}/. The raw LAS/LAZ stays protected; only the derived CSV is served through the authenticated project data route.

2026-06-17 - HCL manual point cloud opening in Cesium instead of Potree
- Symptom: HCL Underground has both a 3D model and manually synced point cloud folders. Clicking/opening the point cloud could switch to the 3D Model/Cesium side instead of staying in Potree.
- Root cause: Dashboard 3D selection waited for an async effect to switch the active canvas tab, so a selected Potree asset could briefly be evaluated while the active tab was still Cesium. Manual detection also only handled some one-level point-cloud folders, leaving nested manual EPT/COPC layouts vulnerable to model discovery.
- Fix: Selecting an item in the 3D sidebar now immediately sets both selected asset and active viewer tab from the asset type. The selected point-cloud URL now trusts selectedAsset.viewer === "potree" directly instead of waiting on tab state. Backend manual sync/project listing now detects nested EPT/COPC folders and excludes any folder containing point-cloud viewer assets from 3D model classification.
- Verification: Backend compile passed: python -m compileall backend/app. Frontend build could not be rerun in this turn because the shell command was blocked by the local Codex usage-limit reviewer.
